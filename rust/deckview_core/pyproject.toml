[build-system]
requires = ["maturin>=1.14,<2.0"]
build-backend = "maturin"

[project]
name = "deckview_core"
version = "0.3.0"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
